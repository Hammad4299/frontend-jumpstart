//Contains settings that might need to be adjusted depending on environment/server/machine where project is running.

const envSettings = {
    public: '/useful-resources/Webpack%20Jumpstart/dist/',
};

export default envSettings;